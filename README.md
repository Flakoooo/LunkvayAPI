# LunkvayAPI

LunkvayAPI — это бэкенд-сервис на ASP.NET Core 9.0 для чат-приложения. Он предоставляет полнофункциональный RESTful API для управления пользователями, профилями, аутентификацией, друзьями и чатами в реальном времени.

## ✨ Возможности

- **Аутентификация:** Регистрация и вход пользователей с использованием JWT (JSON Web Tokens).
- **Управление профилями:** Создание и редактирование профилей пользователей, включая статус и информацию "о себе".
- **Аватары:** Загрузка, получение и удаление аватаров пользователей. Изображения обрабатываются и сохраняются в формате WebP.
- **Система дружбы:** Отправка, принятие/отклонение запросов в друзья, получение списков друзей и управление метками.
- **Чаты:** Создание групповых чатов, получение списка чатов пользователя.
- **Обмен сообщениями в реальном времени:** Интеграция с SignalR для мгновенной доставки сообщений.
- **Изображения чатов:** Загрузка, получение и удаление изображений для групповых чатов.

## 🛠️ Технологии

- **Фреймворк:** C# на .NET 9.0, ASP.NET Core
- **База данных:** MySQL
- **ORM:** Entity Framework Core
- **Аутентификация:** JWT Bearer
- **Real-time:** SignalR
- **Обработка изображений:** SixLabors.ImageSharp
- **Контейнеризация:** Docker, Docker Compose
- **Документация API:** Scalar

## 🚀 Начало работы

### Требования

- [.NET 9.0 SDK](https://dotnet.microsoft.com/download/dotnet/9.0)
- [Docker](https://www.docker.com/products/docker-desktop/) (для запуска в контейнере)

### Запуск с помощью Docker Compose

Это рекомендуемый способ для быстрого запуска приложения вместе с базой данных MySQL.

1.  **Создайте файл `.env`** в корневой директории проекта. Этот файл не отслеживается системой контроля версий и должен содержать ваши секретные данные. Добавьте в него следующие переменные:

    ```env
    JWT_KEY=your_super_secret_jwt_key_with_at_least_32_characters
    JWT_ISSUER=LunkvayAPI
    DB_NAME=lunkvaydb
    DB_USER=user
    DB_PASSWORD=password
    ```

2.  **Запустите Docker Compose**, используя `dev-docker-compose.yml` для разработки:

    ```bash
    docker-compose -f dev-docker-compose.yml up --build
    ```

    API будет доступен по адресу `http://localhost:8080`.

### Запуск локально через .NET CLI

1.  **Настройте базу данных:** Убедитесь, что у вас есть запущенный экземпляр MySQL, и обновите строку подключения в `mainapi/appsettings.Development.json`.

2.  **Восстановите зависимости:**

    ```bash
    dotnet restore
    ```

3.  **Примените миграции базы данных:**

    ```bash
    dotnet ef database update --project mainapi/LunkvayAPI.csproj
    ```

4.  **Запустите приложение:**

    ```bash
    dotnet run --project mainapi/LunkvayAPI.csproj
    ```

## 📁 Структура проекта

-   `/mainapi`: Основной проект ASP.NET Core.
    -   `/Controllers`: Контроллеры API, обрабатывающие HTTP-запросы.
    -   `/Services`: Бизнес-логика приложения.
    -   `/Data`: Контекст базы данных (Entity Framework), миграции и сущности.
    -   `/Models`: DTO (Data Transfer Objects) и модели запросов/ответов.
    -   `/Common`: Общие компоненты, такие как фильтры и утилиты.
-   `/Media`: Директория для хранения загруженных файлов (аватары, изображения чатов). Содержимое этой папки должно быть смонтировано в Docker-контейнер.
-   `*.sln`, `*.csproj`: Файлы проекта Visual Studio.
-   `docker-compose.yml`, `dev-docker-compose.yml`: Файлы для оркестрации контейнеров.

## 📚 Документация API

Интерактивная документация API доступна через Scalar. Когда приложение запущено, перейдите по адресу `/swagger` для просмотра всех конечных точек и их тестирования.